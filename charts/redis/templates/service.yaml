apiVersion: v1
kind: Service
metadata:
  name: {{ include "sudo0x-redis.fullname" . }}
  labels:
    {{- include "sudo0x-redis.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: redis
      port: {{ .Values.service.port }}
      targetPort: redis
      protocol: TCP
  selector:
    {{- include "sudo0x-redis.selectorLabels" . | nindent 4 }}
